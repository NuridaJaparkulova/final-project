import React from 'react';
import Contacts from '../../../components/Layout/Basket/Contacts/Contacts';

const contacts = () => {
    return (
        <div>
            <Contacts/>
        </div>
    );
};

export default contacts;